<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#40403f"
                android:centerColor="#40403f"
                android:centerY="1.0"
                android:endColor="#40403f"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#26b023"
                    android:centerColor="#26b023"
                    android:centerY="1.0"
                    android:endColor="#26b023"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>